-------------------------
live ASL reader (ASLive)
-------------------------
0) pick dataset - https://www.kaggle.com/datasets/ayuraj/asl-dataset
1) explore dataset

coding steps:

model training
1) read in image data
2) preprocess data in prep for a CNN
3) determine a good starting CNN model
4) adjust model and hyperparameter tune
5) test/repeat step 4
6) export model

live image
1) access/use camera
2) take image when hand is "still"?
3) preprocess image same as model training preprocessing
4) feed model the image
